// logical operator 
# include <stdio.h>

int main () {

    // veriables 

    int a = 0;
    int b= 1;
    // using logical operator in print function 

    printf(" according to AND gate : %d\n", a&&b); // this is AND logic operator
    printf(" Accordind to OR gate : %d\n", a||b);  // this is or logic operator 

    // not operator

    printf(" the value of not(a) is %d " , !a) ;   // int a = 0;


    return 0 ;
}