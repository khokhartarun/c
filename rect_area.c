# include <stdio.h>

int main () 
{
    int length , breath ;

    printf( " the length of rectangle is \n");
    scanf( "%d\n", &length);

    printf( " enter the brethof the rectangle \n");
    scanf( "%d\n", &breath);

    printf( " the final area is %d\n", length*breath);

    return 0;
}